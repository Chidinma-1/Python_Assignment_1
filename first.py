# text = """ An alternative is designed development, in which high-level insight into the problem can make the programming
# much easier. In this case, the insight is that a Time object is really a three-digit number in base 60"""


# search_for = input("> ").lower()

# print(f"{text.lower().count(search_for)} results(S) found")
# print(text.lower().replace(search_for, search_for.upper()))
